# CS2 AI Demo Coach 首版开发范围

> 文档性质：首版开发清单与砍需求依据，首版完成后归档
> 版本：0.5
> 日期：2026-08-24
> 产品目标：[PRD.md](./PRD.md)
> 技术实现依据：[ARCHITECTURE.md](./ARCHITECTURE.md)

## 1. 首版唯一要验证的事情

验证玩家是否认可并愿意继续使用这样一种体验：

> 上传一场 Demo 后，不是收到几条分析结论，而是由 AI 像高手主播一样带着自己过完整场比赛；普通片段快速带过，关键处理先完整看完，再回到决策点复盘，全部看完后再做总结。

首版成功不取决于发现了多少问题，而取决于用户是否认为“真的有人认真陪我看完了这场 Demo，并讲清了最重要的问题”。

## 2. 首版体验边界

| 维度 | 首版范围 |
|---|---|
| 游戏 | Counter-Strike 2 |
| 地图 | Mirage |
| 用户 | 单名玩家、单场 Demo |
| 阵营 | 同一玩家在整场比赛中的 T、CT 两方内容 |
| 文件 | 单个 `.dem`，或只包含一个 `.dem` 的 `.zip` |
| 导入 | 用户手工上传，不接平台账号和自动导入 |
| 主界面 | 浏览器在线 2D 战术回放 |
| 核心交付 | 一次覆盖全场的 AI 引导复盘会话 |
| 讲解形式 | 文字教练窗口、时间轴、地图标记、轨迹和局面卡片 |
| 语音 | 不做 TTS，也不识别队内语音 |
| 职业案例 | 有可靠相似样本时展示；样本不足时明确说明，不强行匹配 |
| 复盘总结 | 完成整场带看后生成，不提供绕过带看的“先看结论”入口 |
| 账号 | 无账号；使用不可猜测的会话链接恢复进度 |
| 支付 | 不接真实支付，只收集再次使用和付费意愿 |
| 个人记忆 | 不做跨 Demo 记忆，只在当前会话内识别重复问题 |
| 部署 | 单环境、单区域，不承诺高可用 |

## 3. 用户流程

### 3.1 上传 Demo

页面必须提供：

- 支持范围和隐私说明；
- `.dem` / `.zip` 拖拽上传；
- 文件类型、大小和压缩安全校验；
- 上传失败后的明确原因与重试入口。

### 3.2 选择自己

解析出双方玩家后，用户选择要复盘的账号。页面显示昵称、Steam ID（Demo 中存在时）、阵营和基础数据，避免重名误选。

### 3.3 准备复盘

系统依次展示真实阶段：

1. 解析比赛；
2. 重建 2D 时间轴；
3. 识别关键局面；
4. 编排复盘路线；
5. 准备教练讲解。

不显示无法兑现的虚假百分比。用户离开页面后可通过会话链接回来。

### 3.4 AI 带看完整场比赛

进入会话后，系统先用一句话说明本场结构和预计复盘时间，然后从第一回合开始推进到最后一回合。

每一回合必须属于以下一种处理方式：

- `SKIP`：冻结时间、长时间无有效信息或与所选玩家无关的尾段；在时间轴标明跳过范围和原因；
- `BRIEF`：普通回合，用一句话交代发生了什么后快速推进；
- `OBSERVE`：值得用户留意但不需要长讲的片段；
- `DEEP_DIVE`：从关键选择前约 1 秒连续播放完整处理和结果，再自动回到决策点统一讲解；
- `HABIT_CHECK`：此前讲过的问题再次出现，先看完整处理，再回到决策点简短点明同一模式与主动作。

系统可以加速和跳转，但不能让任何回合在复盘路线中消失。用户始终能看到当前位置、已跳过内容和剩余内容，也可以展开被跳过片段。

### 3.5 完成后总结

只有到达最后一回合后，才进入总结：

- 本场最影响表现的 1–3 个习惯；
- 每个习惯在本场出现的次数与代表回合；
- 一个做得好的地方；
- 下一场只需要重点执行的 1–2 个目标；
- 用户对讲解准确性、帮助程度和付费意愿的反馈。

总结是复盘会话的收尾，不是产品的替代入口。

## 4. 复盘会话的 P0 交互

### 4.1 页面布局

- 版本化真实 Mirage 雷达为主区域；10 名玩家的位置、存活与朝向按 canonical tick 同步，支持缩放/平移；
- 底部为完整比赛时间轴，按半场、回合和讲解点分层；
- 右侧为 AI 教练窗口，显示当前讲解、可用操作和追问入口；
- 顶部显示比分、回合、时间、阵营、存活、经济与炸弹状态；
- 玩家列表或详情显示当前生命、护甲、手持物、库存/道具、金钱、拆弹器与 C4；所有值随播放 tick 同步；
- 投掷物显示飞行路径与生效范围，击杀、伤害、开火和炸弹事件可在时间轴与地图上定位；
- 地图始终显示当前 tick 的全知 cs2d 回放，不提供“玩家已知 / 全知”切换；玩家信息边界只用于内部规则与 LLM 证据；
- 用户可播放、暂停、前后跳 5 秒、调速、回到讲解点和展开跳过片段。

### 4.2 深度讲解脚本

每个 `DEEP_DIVE` 至少包含：

1. **前置上下文**：从玩家仍能选择的时刻前约 1 秒进入片段；
2. **完整处理**：不中断地播放玩家实际动作和后续可验证结果；
3. **自动回看**：结果结束后自动回到决策点暂停；
4. **当前情况**：用蓝色信息带展示比分、人数、时间、位置、生命护甲、武器道具、经济和当时可知敌情；
5. **你做了什么**：用红色信息带只描述 Demo 能证明的 Outcome Fact；
6. **教练分析**：用绿色信息带给出具体判断、一个优先动作和触发条件；
7. **证据补充**：有可靠职业样本时展示动作分布或相似回合；
8. **确认理解**：允许用户再看一遍、追问或继续下一段。

不能只在玩家死亡后说“这里不该 peek”，也不能以最终输赢倒推选择必然错误。

### 4.3 当前局面追问

首版只支持围绕当前讲解点的文本追问，例如：

- “为什么不能继续架这里？”
- “如果队友报了两个 A 呢？”
- “职业选手这里一般怎么做？”
- “能再放一遍吗？”

回答只能使用当前时刻之前的事实、已计算的分析和检索到的证据。系统不知道语音或战术安排时必须直说，并允许用户补充信息后给条件化回答。

### 4.4 会话节奏

- 目标总时长：15–30 分钟；
- 全场最多安排 50 个有实际教学价值的 `DEEP_DIVE` / `HABIT_CHECK`；实际数量由负向结果、风险证据和同回合去重决定，不为凑满上限硬塞片段；
- 同类问题再次出现时优先使用 `HABIT_CHECK`，避免重复长讲；
- 普通回合用 2–8 倍速或摘要带过；
- 用户可以随时接管播放，但系统应能回到当前讲解点继续主持。

## 5. 首版分析能力

### 5.1 Demo 解析

必须抽取：

- 比赛、地图、回合、阵营、比分、tick 和时钟；
- 玩家身份，以及随 tick/变化点变化的阵营、存活、world X/Y/Z、视角、速度、生命、护甲、头盔和金钱；
- 当前手持物（枪、刀、道具或 C4）、完整库存/道具数量、弹药、拆弹器、装备价值和 C4 携带状态；
- 击杀、伤害、开火、换弹、炸弹和常见道具事件；
- 双方轨迹、队友关系、经济和装备档位；
- 可获得时的 spotted / spotted-by 信息；
- 解析器可获得时的脚步/player sound、枪声等声音发射事件；声音事件不等于某玩家确定听到；
- Demo 哈希、解析器版本和游戏版本线索。

可不解析：队内语音、鼠标原始输入、聊天、精确压枪轨迹和原始音频波形。解析器缺少某类声音或装备字段时必须显式标记为不可得，不能补造。

### 5.2 Mirage 地图语义

必须有版本化的：

- 与 CS2 地图构建版本对应的真实 Mirage 雷达资源、内容哈希、来源与权利状态；
- CS2 world X/Y/Z 到雷达像素的固定标定与锚点回归测试；
- 中文点位、导航区域与区域多边形；
- A/B 包点、入口、连接区、默认站位和常见回防路径；
- 区域相邻关系；
- 简化视线、掩体和补枪距离判断接口；
- 轨迹、朝向、手持/装备、道具路径和事件在真实雷达上的稳定渲染坐标。

### 5.3 当时可知信息

建议生成不得读取未来事件或未被观察的敌人真实位置。虽然用户地图始终显示全知回放，内部 `ObservableState` 仍必须是带来源、时间、空间精度、身份精度、置信度和过期规则的多维信息状态，不能用一个可见圆或布尔值表示，也不能因为地图已显示就进入 LLM。信息来源包括：

1. Demo 中可靠的 spotted 状态；
2. 玩家直接视觉确认的敌人；
3. 脚步、枪声、伤害方向、道具和炸弹等事件形成的方向/区域信息；
4. 随时间衰减、可能范围扩大的最后已知位置；
5. 有可靠共享机制或用户明确补充的队友报点/战术上下文。

全知回放显示所有玩家的真实位置、当前手持和库存；内部决策证据只包含当时有依据知道的内容。脚步事实不能直接变成隐藏敌人的精确坐标并送入决策侧 LLM；队友看见也不自动等于所选玩家知道。无法重建的语音、声学遮挡和队伍战术属于明确限制，强依赖这些信息的判断必须降低置信度或改为条件化讲解。

### 5.4 首版重点局面

首版优先深讲四类最容易从 Demo 证实、也最适合 2D 表达的问题：

1. 人数优势后前压、重复 peek、换位或后撤；
2. 接触发生后的站位、拖延、补枪关系和生存选择；
3. 转点、回防、保枪的启动时机与路线；
4. 关键道具的使用时机、目的和对局面产生的直接影响。

经济、准星、枪法、复杂战术意图可以作为背景事实，不作为首版主要教学能力。

### 5.5 复盘路线编排

系统先产生候选讲解点，再按以下因素选择深讲内容：

- 对回合和整场的影响；
- 玩家是否拥有真实选择；
- 能否从 2D 画面和事实解释；
- 是否具有可执行替代方案；
- 是否与本场其他时刻形成重复习惯；
- 是否有高质量职业样本；
- 是否与此前讲解重复。

路线必须覆盖从第一回合到最后一回合的连续时间区间，不能有未标注空洞或重叠冲突。

### 5.6 职业行为证据

首版使用结构化职业局面数据库和规则检索，不训练端到端模型。

- 先按地图、阵营、区域、回合阶段、人数和炸弹状态硬过滤；
- 再按时间、装备、队友覆盖、已知敌情、道具和近期事件排序；
- 样本 `n >= 20` 才展示动作比例；
- `5 <= n < 20` 只展示案例并标注样本较少；
- `n < 5` 不声称“职业选手通常会怎样”；
- 不用回合最终胜负直接证明一个动作正确。

没有职业证据不应阻塞讲解。系统可以只基于可验证事实和教练规则给出低风险建议。

## 6. 明确不做

- 不提供上传后直接阅读的多问题报告；
- 不控制本地 CS2 客户端，不做悬浮窗；
- 不分析实时对局，不读取游戏进程内存；
- 不做跨 Demo 的个人习惯记忆；
- 不做所有地图、所有模式或队伍级战术分析；
- 不做语音教练、虚拟人或自动剪辑视频；
- 不训练强化学习、行为克隆或端到端 Demo 大模型；
- 不接账号体系、支付、订阅、排行榜和社交分享；
- 不承诺职业选手姓名级风格模仿；
- 不允许 LLM 自主读取原始 tick、计算事实或杜撰证据。

## 7. 验收标准

### 7.1 功能验收

- 一份支持范围内的 Demo 能完成上传、解析和目标玩家选择；
- 2D 回放能在真实 Mirage 雷达上从第一回合稳定播放到最后一回合；
- 任取固定 tick，10 名玩家的位置/朝向、存活/生命护甲、当前手持、库存道具、经济与 C4 状态能追溯到 Demo 事实或显式不可得字段；
- 投掷物路径、生效范围、击杀与炸弹事件可与 canonical tick 同步播放；
- cs2d 地图在所有阶段显示当前 tick 全知事实；决策侧 LLM 输入与该 renderer 数据严格分离，并通过 ObservableState/claim 引用做未来信息回归；
- 时间轴上的所有比赛区间均被 `SKIP`、`BRIEF`、`OBSERVE`、`DEEP_DIVE` 或 `HABIT_CHECK` 覆盖；
- 跳过内容有可见原因，用户可展开；
- 至少 4 个关键点能完成“约 1 秒前置上下文—完整结果播放—回到决策点—三段复盘”流程；
- 当前局面追问不会使用未来信息或虚构语音；
- 相同输入和相同版本可生成相同事实与复盘路线；
- 用户完成最后一回合后才能看到基于本次带看内容生成的总结；
- 解析、分析或 LLM 失败时给出可恢复状态，不丢失已完成结果。

### 7.2 体验验收

邀请至少 10 名目标玩家完成不受开发者口头引导的测试：

- 至少 8 人能独立完成上传并进入复盘；
- 至少 7 人完成整场带看；
- 至少 7 人同意“它像有人带我看 Demo，而不是给我一份报告”；
- 至少 6 人能在结束后复述下一场要改变的具体行为；
- 至少 5 人明确愿意再次上传 Demo；
- 记录愿意支付的价格，不以假设性点击代替访谈。

### 7.3 质量红线

出现任一项即不允许对外测试：

- 2D 回放与实际回合、位置或事件明显错位；
- 仍使用示意地图或百分比手调坐标冒充真实 Mirage 回放；
- 把脚步/枪声或队友全知坐标无条件送进决策侧规则/LLM，冒充所选玩家当时证据；
- 某 tick 的玩家手持、库存、生命护甲或 C4 状态与 Demo 明显不符；
- 讲解使用决策发生后的信息冒充玩家当时已知信息；
- 跳过未在时间轴说明，造成“完整看过”的错误印象；
- 职业比例无法追溯到样本；
- 把推断写成确定事实；
- 当前局面追问可以诱导系统编造证据。

## 8. 推荐开发顺序

### 阶段 0：人工验证讲解体验

- 选取 5 场 Mirage Demo；
- 人工编排完整复盘路线和 15 个讲解点；
- 用静态 2D 原型验证“完整播放—回到决策点—统一解释—追问—继续”的节奏；
- 固化讲解模板和用户最常问的问题。

### 阶段 1：可靠解析与整场 2D 回放

- 上传、校验、解析、玩家选择；
- 标准比赛时间轴与 Mirage 地图语义；
- 完整 2D 播放、跳转、调速和回合导航；
- 用测试 Demo 建立解析回归集。

### 阶段 2：关键局面与复盘路线

- 四类重点局面检测；
- `ObservableState` 与未来信息隔离；
- 候选评分、重复问题聚类和全场路线编排；
- 跳过/简述/观察/深讲的可解释选择。

### 阶段 3：会话主持与讲解

- 会话状态机和播放器控制协议；
- 完整处理播放、自动回到决策点、三段复盘、地图标记和结果重播；
- 当前局面问答；
- 职业证据检索与样本不足降级。

### 阶段 4：总结、反馈与封闭测试

- 基于已讲内容的会后总结；
- 会话恢复、错误重试和反馈采集；
- 10 名目标用户测试；
- 依据完成率和访谈决定是否扩地图、做语音或开始桌面端。

## 9. 砍需求规则

新需求只有同时满足以下条件才进入首版：

1. 直接改善“AI 带用户过完整场 Demo”的核心体验；
2. 能在 Mirage 单场 Demo 上验证；
3. 不需要新增高风险数据源、客户端注入或独立基础设施；
4. 有清晰验收方法；
5. 不挤压解析正确性、2D 回放和会话主持的开发时间。

遇到排期冲突时，优先级为：

1. 回放与事实正确；
2. 完整路线与会话控制；
3. 关键时刻讲解质量；
4. 当前局面问答；
5. 职业案例；
6. 视觉润色和扩展功能。

## 10. 完成定义

首版完成必须同时满足：

- 功能验收全部通过；
- 体验测试达到门槛；
- 质量红线回归测试通过；
- 支持范围、数据保留和已知限制在产品内可见；
- 部署、备份、失败重试和异常处理已演练；
- [ARCHITECTURE.md](./ARCHITECTURE.md) 已按实际实现更新；
- 产品团队能用一句话真实描述交付物：**AI 带玩家看完一场 Demo，而不是 AI 给玩家几条分析。**
