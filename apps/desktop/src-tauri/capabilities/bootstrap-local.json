{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "bootstrap-local",
  "description": "Narrow commands for the bundled bootstrap window only.",
  "local": true,
  "windows": ["bootstrap"],
  "permissions": [
    "allow-runtime-status",
    "allow-open-settings",
    "allow-runtime-restart"
  ]
}
