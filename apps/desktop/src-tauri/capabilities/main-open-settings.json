{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-open-settings",
  "description": "Allows the protected desktop coaching page to open the bundled Settings window and nothing else.",
  "local": false,
  "remote": {
    "urls": ["http://127.0.0.1:*/*"]
  },
  "windows": ["main"],
  "permissions": [
    "allow-open-settings"
  ]
}
