# 真实带看可靠性验证（2026-09-25）

基线：a1d5858，分支codex/jev-decision-assessment。使用已有test_demo.dem（9回合、10名玩家），Edge InPrivate，localhost Host＋已构建静态Viewer。Demo/Replay留在本机页面及Worker；Jev/DeepSeek凭据为空，RULE_BASELINE，Memory关闭。

## 实际修复与对照

| 路径 | Before | After |
|---|---|---|
| 正常选择Demo与玩家、等待编排 | 本地CS-Net与前两段讲解完成后，卡在“正在提交可恢复起点”；Zod拒绝decision_assessment_run | 显式接纳可选、有界审计字段，实际页面进入默认带看并在首cue结果后停靠 |
| 已看首cue→后退15秒→讲解最近教练点 | R1回访不建立新结果门，持续播放进入R2，界面进度变2/4 | 完整播放后停回同一R1决策点；不跨cue，默认进度保持1/4 |
| 回访后回到默认顺序、再看一遍 | 默认游标为保留目标，不作为本轮新缺陷 | 回原讲解卡，可重播且再次停回决策点 |
| 刷新→重选同一Demo | 本轮修复前启动已阻塞，无法形成新记录 | 自动锁定原玩家，恢复首cue暂停及讲解；点击继续可进入R2，后续cue正常停靠 |

实际随后继续R2、R3、R9其余教学点，覆盖路线直到R9末尾100%；全场总结出现后点击“完成本次复盘”，页面显示“复盘完成”。所有反思提问均显式选择“跳过，直接看分析”，没有伪造玩家意图。

恢复检查不是新一轮编排：保存的冻结route/audit由本地恢复路径读取；恢复日志未出现新的assess-decision或Director请求。浏览器兼容路径仅保留有限讲解缓存，后续未缓存cue仍可能触发确定性Narrator请求，不声称整场零请求。所有这些请求的远端模型provider都关闭。

## 自动验证

- 新的实际编排→恢复回归：修复前1失败/15通过，失败与页面Zod字段完全一致；修复后通过。
- 新的已呈现cue回访reducer回归：修复前1失败/5通过，预期REVEALING＋LOCKED，实际PLAYING＋缺门；修复后通过。
- Session、Coaching Host、Recovery、Agent manual、恢复schema：28文件274测试通过。
- 历史库和Host历史恢复：7文件46测试通过（隔离SQLite，不使用用户资料库）。
- 额外恢复重连、客户端打包边界等检查：7文件58测试通过，和前述部分重叠，不累加计数。
- Desktop runtime：14测试通过。
- TypeScript：通过。
- 补充准备错误取消回归后，编排集成文件16测试通过，确认READY_TO_START被Host取消后不会继续准备晚到讲解覆盖错误状态。
- 生产构建：`pnpm build`通过（Next编译、TypeScript、页面生成）；改动后的独立`pnpm typecheck`通过。

## 限制与下一轮

本轮证明实际启动/播放/恢复可靠性改善，未做Jev与生成模型的新付费对照，不能证明专业判断质量提升。没有启动桌面App验证SQLite历史列表UI，也没有运行打包sidecar smoke；本次桌面覆盖为隔离SQLite与runtime测试。没有重建未修改的Viewer。页面验收使用开发Host＋静态Viewer，生产产物只做构建校验。开发热更新曾重新显示恢复提示，刷新后恢复路径已独立走通。测试用例的合成tick不作为真实Demo tick证据；真实页面的停靠时间只用其解析后播放状态核对。

下一轮只建议处理**暂停与自由查看的语义混淆**：真实R2默认播放点击底部暂停立即进入“自由查看”；再点播放仍停留该模式，必须另点“回到默认顺序”才能恢复教练。代码同样显示issueUserCommand→markUserTookOver。建议为普通暂停/播放保留带看意图，并单独验证结果窗口中断与续播；本轮未修改它。

清理：本轮Edge InPrivate窗口已关闭；唯一控制器已停止，3000/5174无监听；所有测试/构建进程结束。未安装应用、部署或修改用户主工作树。
